import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.matthieufranques.devisfactures',
  appName: 'Devis & Factures',
  webDir: 'dist'
};

export default config;
